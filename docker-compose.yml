version: '3'

services:
  v2ex-monitor:
    build: .
    container_name: v2ex-monitor
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./appdata:/appdata
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3" 
    networks:
      - v2ex-monitor

networks:
  v2ex-monitor:
    driver: bridge